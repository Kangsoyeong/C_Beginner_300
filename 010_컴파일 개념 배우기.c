//잘못된 문장을 작성하여 컴파일 에러를 발생시켜 보세요.
#include <stdio.h>
int main(void) {
	printf("hello")
		return 0;
}

/*
컴파일 :

사람이 이해하는 언어를 컴퓨터가 이해할 수 있는 언어로 바꿔주는 과정.
printf("hello");와 같은 원시코드(.c, .java 등)를 기계가 이해할 수 있는 목적파일(.obj)과 실행파일(.exe)로 바꿔준다. 
*/