//변수 x,y를 정의하고, x의 값은 +4로, y의 값은 -2로 초기화한 후 x+y와 x-y의 결과를 출력하는 프로그램을 작성하세요.

#include <stdio.h>
int main(void) {

	int x = 4;
	int y = -2;

	printf("%d\n", x+y);
	printf("%d", x-y);

	return 0;
}